# Database (required)
DATABASE_URL=postgresql://user:password@localhost:5432/manna
PGHOST=localhost
PGPORT=5432
PGUSER=user
PGPASSWORD=password
PGDATABASE=manna

# Server
PORT=5000
SESSION_SECRET=change-me-to-a-random-string

# Deployment (optional - used for OpenGraph meta image URLs)
# BASE_URL=https://yourdomain.com
